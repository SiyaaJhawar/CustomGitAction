name: Test

on:
  pull_request:
    types: [opened]
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - uses: ./
      - run: npm install octokit
    
      
